namespace engine {
namespace Window {

enum Type { SCENE, PROPERTIES, MATERIALS, DRAWING_TOOLS, HISTOGRAM, CAMERA, TEXTURE, IMAGE};

static const Type All[] = {SCENE, PROPERTIES, MATERIALS, DRAWING_TOOLS, HISTOGRAM, CAMERA, TEXTURE, IMAGE};

}  // namespace Window
}  // namespace engine